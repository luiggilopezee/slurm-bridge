---

# -- Overrides the name of the release.
nameOverride: ""

# -- Overrides the full name of the release.
fullnameOverride: ""

# -- Overrides the namespace of the release.
namespaceOverride: ""

# Default values for slurm-bridge.
scheduler:
  # -- The image to use, `${repository}:${tag}`.
  # Ref: https://kubernetes.io/docs/concepts/containers/images/#image-names
  image:
    repository: ghcr.io/slinkyproject/slurm-bridge-scheduler
    tag: ""
    # -- Image pull policy.
    pullPolicy: IfNotPresent
  # -- Set the number of replicas to deploy.
  replicaCount: 1
  # -- PodDisruptionBudget for the scheduler deployment
  pdb:
    # -- Enable PodDisruptionBudget
    enabled: false
  # -- Enables leader election.
  leaderElect: false
  # -- Scheduler feature gates. SlurmBridgeGenericWorkload requires a supported
  # built-in Workload and PodGroup API at startup; disable it for clusters without
  # those APIs. It is independent of Kubernetes' GenericWorkload feature gate.
  featureGates:
    DRAExtendedResource: true
    DynamicResourceAllocation: true
    SchedulerPopFromBackoffQ: false
    SlurmBridgeGenericWorkload: true
  # -- Set the priority class to use.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#priorityclass
  priorityClassName: ""
  # -- Set container resource requests and limits for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-requests-and-limits-of-pod-and-container
  resources: {}
    # requests:
    #   cpu: 1
    #   memory: 1Gi
    # limits:
    #   cpu: 2
    #   memory: 4Gi
  # -- (map[string]string) Node label selector for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector
  nodeSelector: {}
  # -- Set affinity for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity
  affinity: {}
  # -- Configure pod tolerations.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
  tolerations: []
  # -- (integer) Set the verbosity level of the scheduler.
  verbosity: null

# Configuration settings for the scheduler.
schedulerConfig:
  # -- Set the name of the scheduler.
  schedulerName: slurm-bridge-scheduler
  # -- Set the Slurm MCS Label to use for external jobs.
  # Ref: https://slurm.schedmd.com/sbatch.html#OPT_mcs-label
  mcsLabel: kubernetes
  # -- Set the default Slurm partition to use for external jobs.
  # Ref: https://slurm.schedmd.com/sbatch.html#OPT_partition
  partition: slurm-bridge

# Configuration settings for the admission controller.
admission:
  # -- Enables admission controller.
  enabled: true
  # -- The image to use, `${repository}:${tag}`.
  # Ref: https://kubernetes.io/docs/concepts/containers/images/#image-names
  image:
    repository: ghcr.io/slinkyproject/slurm-bridge-admission
    tag: ""
    # -- Image pull policy.
    pullPolicy: IfNotPresent
  # Cert-Manager certificate configurations.
  certManager:
    # -- Enables cert-manager for certificate management.
    enabled: true
    # -- Duration of certificate life.
    duration: 43800h0m0s # 5 year
    # -- Certificate renewal time. Should be before the expiration.
    renewBefore: 8760h0m0s # 1 year
  # -- Set the number of replicas to deploy.
  replicas: 1
  # -- PodDisruptionBudget for the admission deployment
  pdb:
    # -- Enable PodDisruptionBudget. Only rendered when `replicas` is greater
    # than 1, since a PDB over a single replica blocks node drains.
    enabled: false
     # -- Minimum pods that must remain available after eviction (int or quoted percent).
    minAvailable: 1
    # -- Maximum pods that may be unavailable (int or quoted percent). Rendered only when set, and takes precedence over `minAvailable`.
    maxUnavailable: null
  # -- Set the priority class to use.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#priorityclass
  priorityClassName: ""
  # -- Set container resource requests and limits for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-requests-and-limits-of-pod-and-container
  resources: {}
    # requests:
    #   cpu: 1
    #   memory: 1Gi
    # limits:
    #   cpu: 2
    #   memory: 4Gi
  # -- (map[string]string) Node label selector for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector
  nodeSelector: {}
  # -- Set affinity for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity
  affinity: {}
  # -- Configure pod tolerations.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
  tolerations: []
  # -- List of namespaces to be monitored by the pod admission controller.
  # Pods created in any of these namespaces will have their `.spec.schedulerName`
  # changed to slurm-bridge.
  managedNamespaces:
    - slurm-bridge
    # - managedNamespace2
  # -- A label selector to select namespaces to be monitored by the pod admission controller.
  # If this is set, managedNamespaces will be ignored.
  # Ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors
  managedNamespaceSelector: {}

# Configuration settings for the controllers.
controllers:
  # -- The image to use, `${repository}:${tag}`.
  # Ref: https://kubernetes.io/docs/concepts/containers/images/#image-names
  image:
    repository: ghcr.io/slinkyproject/slurm-bridge-controllers
    tag: ""
    # -- Image pull policy.
    pullPolicy: IfNotPresent
  # -- Set the number of replicas to deploy.
  replicas: 1
  # -- Enables leader election.
  leaderElect: false
  # -- PodDisruptionBudget for the controllers deployment
  pdb:
    # -- Enable PodDisruptionBudget. Only rendered when `replicas` is greater
    # than 1, since a PDB over a single replica blocks node drains.
    enabled: false
     # -- Minimum pods that must remain available after eviction (int or quoted percent).
    minAvailable: 1
    # -- Maximum pods that may be unavailable (int or quoted percent). Rendered only when set, and takes precedence over `minAvailable`.
    maxUnavailable: null
  # -- Set the priority class to use.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#priorityclass
  priorityClassName: ""
  # -- Set container resource requests and limits for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-requests-and-limits-of-pod-and-container
  resources: {}
    # requests:
    #   cpu: 1
    #   memory: 1Gi
    # limits:
    #   cpu: 2
    #   memory: 4Gi
  # -- (map[string]string) Node label selector for pod assignment.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector
  nodeSelector: {}
  # -- Set affinity for Kubernetes Pod scheduling.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity
  affinity: {}
  # -- Configure pod tolerations.
  # Ref: https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/
  tolerations: []
  # -- (integer) Set the verbosity level of the controllers.
  verbosity: null

# Configurations shared among all components.
sharedConfig:
  # -- The Slurm REST API URL in the form of: `[protocol]://[host]:[port]`
  slurmRestApi: http://slurm-restapi.slurm:6820
  # -- DRA DeviceProfiles recognized by Slurm Bridge. A DeviceClass resolves to
  # a profile when its single CEL selector exactly matches `selector`.
  # `name` becomes the Slurm GRES type and must remain stable while allocations
  # using the profile exist. `driver` must be a Kubernetes DRA driver name and
  # `backend.type` must be `core-bitmap` or `indexed-gres`; indexed GRES names
  # must be DNS-1123 labels of at most 60 characters. Leave this `null` to use
  # the built-in profiles shown below, or set it to `[]` to disable all
  # DeviceProfiles explicitly.
  deviceProfiles: null
  # deviceProfiles:
  #   - name: cpu
  #     driver: dra.cpu
  #     selector: device.driver == "dra.cpu"
  #     backend:
  #       type: core-bitmap
  #   - name: gpu-example
  #     driver: gpu.example.com
  #     selector: device.driver == 'gpu.example.com'
  #     backend:
  #       type: indexed-gres
  #       gresName: gpu
  #   - name: gpu-nvidia
  #     driver: gpu.nvidia.com
  #     selector: >-
  #       device.driver == 'gpu.nvidia.com' &&
  #       device.attributes['gpu.nvidia.com'].type == 'gpu'
  #     backend:
  #       type: indexed-gres
  #       gresName: gpu
  #   - name: dranet-rdma
  #     driver: dra.net
  #     selector: >-
  #       device.driver == 'dra.net' &&
  #       has(device.attributes['dra.net'].pciAddress) &&
  #       has(device.attributes['dra.net'].rdma) &&
  #       device.attributes['dra.net'].rdma == true
  #     backend:
  #       type: indexed-gres
  #       gresName: nic
  # -- The Secret containing the Slurm JWT in the `auth-token` key. The token is
  # mounted and read before each Slurm REST API request to support rotation.
  slurmJwtSecret: slurm-bridge-token
